from setuptools import setup, find_packages

setup(
    name="Rosmaster_Lib",
    version="3.3.9",
    packages=find_packages(),
    install_requires=[],
)

# cd py_install
# sudo python3 setup.py install
